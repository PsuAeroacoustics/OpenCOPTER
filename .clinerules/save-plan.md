# Save Plan

When the user switches to Act Mode, save the current task plan to a markdown file called `.plans/task-<id>-plan.md`, where `<id>` is set based on the current task. This file should then be referenced during the execution of the task.
